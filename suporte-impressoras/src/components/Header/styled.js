import styled from "styled-components";

export const ButtonHeader = styled.span `
  cursor: pointer;
  color: whitesmoke;
  font-weight: bold;
  transition: color 0.3s;
 &:hover{
    transform: scale(1.09);
    cursor: pointer;
    color: var(--white);
  }
`